<?xml version="1.0" encoding="utf-8"?>
<!--
  Exclude the DataStore file (all app data: saved game, best scores, settings) from
  cloud backup and device transfer. PRIVACY.md promises that no data ever leaves the
  device and that everything is deleted on uninstall; Google also warns against
  auto-backup of DataStore files, which can restore truncated/corrupt data.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="file" path="datastore/" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="datastore/" />
    </device-transfer>
</data-extraction-rules>
